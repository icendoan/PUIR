name = "PUIR for 3.04 MP" # name of your mod, shown in launcher
path = "mod/PUIR_304b" # location of your mod files 
user_dir = "PUIR_304b" # optional. creates a sub directory where settings/map cache/savegames etc can be safely written for your mod and not get mixed up
